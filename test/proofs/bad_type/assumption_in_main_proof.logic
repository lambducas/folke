q->r|-(p->q)->(p->r){
    1: assume q->r;
    2: {
        3: assume p->q;
        4: {
            5: assume p;
            6: IfE[5, 3] q;
            IfE[6,1] r;
        }
        IfI[4] p->r;
    }
    IfI[2] (p->q)->(p->r);
}